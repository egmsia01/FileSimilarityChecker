    private static String getHash(File file) throws IOException, NoSuchAlgorithmException {
        MessageDigest md = MessageDigest.getInstance("SHA-256");
        FileInputStream fi = new FileInputStream(file);
        byte[] dataBytes = new byte[1024];
        int nread = 0;
        while ((nread = fi.read(dataBytes)) != -1) {
            md.update(dataBytes, 0, nread);
        }
        fis.close();
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < mdytes.length; i++) {
            sb.append(Integer.toString((mdytes[i] & 0xff) + 0x100, 16).substring(1));
        }
        return sb.toString();
    }